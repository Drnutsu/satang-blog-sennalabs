import { configReducer, configActions } from './reducers'
import configSagas from './sagas'
import * as configSelectors from './selectors'

export { configReducer, configActions, configSagas, configSelectors }
